apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ms-browse.fullname" . }}-test-connection"
  labels:
    {{- include "ms-browse.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ms-browse.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
